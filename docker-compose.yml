version: '3.8'

services:
  api:
    build: .
    container_name: clouddeploy-api-server
    ports:
      - "5000:5000"
    environment:
      - ENVIRONMENT=docker-compose-local
      - APP_VERSION=1.1.0